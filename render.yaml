services:
  - type: web
    name: jairtimes
    runtime: node
    region: us-oregon
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: true
